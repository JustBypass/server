#include <string>
#include <set>

class meter{
public:
    int* get_sizes(std::string from,
                    std::set<std::string> to,
                    std::string body);

    int* get_sizes(std::string from,
                    std::string to,
                    std::string body);       

};